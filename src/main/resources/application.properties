# -----------------------------
# Spring Boot Application
# -----------------------------
spring.application.name=auth-backend
spring.profiles.active=dev
server.port=9090

spring.jpa.open-in-view=false
# -----------------------------
# Admin Credentials
# -----------------------------
ADMIN_EMAIL=admin@hec.gov.pk
ADMIN_PASSWORD=admin123

# -----------------------------
# IPFS Configuration
# -----------------------------
# Choose IPFS mode: local OR infura
ipfs.mode=local

# Local IPFS
ipfs.local.gateway.url=http://127.0.0.1:5001/api/v0
ipfs.gateway.url=http://127.0.0.1:5001/api/v0

# Infura IPFS (Optional)
ipfs.infura.gateway.url=https://ipfs.infura.io:5001/api/v0
ipfs.infura.project.id=YOUR_INFURA_PROJECT_ID
ipfs.infura.project.secret=YOUR_INFURA_PROJECT_SECRET

# -----------------------------
# JSearch API Configuration
# -----------------------------
jsearch.api.key=7d406bdf4emsh3de9d75c3263ffap1c353bjsn78add05e7dea
jsearch.api.host=jsearch.p.rapidapi.com

# -----------------------------
# Security - JWT Configuration
# -----------------------------
security.jwt.secret=${JWT_SECRET:saiofal;vkasfbehwqpofhjashofjaswfpojasfvbafop[hfifonfo[shjfwefjjfslajfsahfoshffasnbash[f}
security.jwt.issuer=${JWT_ISSUER:auth-backend}
security.jwt.access-ttl-seconds=${JWT_ACCESS_TTL_SECONDS:900}
security.jwt.refresh-ttl-seconds=${JWT_REFRESH_TTL_SECONDS:1209600}
security.jwt.refresh-cookie-name=${JWT_REFRESH_COOKIE_NAME:refresh_token}
security.jwt.cookie-secure=${JWT_COOKIE_SECURE:true}
security.jwt.cookie-same-site=${JWT_COOKIE_SAME_SITE:Lax}


# -----------------------------
# Database Configuration
# -----------------------------
spring.datasource.url=jdbc:mysql://localhost:3306/careerkey
spring.datasource.username=root
spring.datasource.password=root
spring.datasource.hikari.pool-name=neon-hikari
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.idle-timeout=30000
spring.datasource.hikari.connection-timeout=20000
spring.datasource.hikari.max-lifetime=1800000
spring.datasource.hikari.validation-timeout=5000
spring.datasource.hikari.leak-detection-threshold=0
spring.datasource.hikari.initialization-fail-timeout=-1
spring.jpa.hibernate.ddl-auto=update

#railway database configuration
# -----------------------------
# Database Configuration (Railway MySQL)
# -----------------------------
# spring.datasource.url=jdbc:mysql://gondola.proxy.rlwy.net:14738/railway?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
# spring.datasource.username=root
# spring.datasource.password=tMYLGbXylqyVMKleXFeZwTXWpHOdiEho

# spring.datasource.hikari.pool-name=neon-hikari
# spring.datasource.hikari.maximum-pool-size=10
# spring.datasource.hikari.minimum-idle=2
# spring.datasource.hikari.idle-timeout=30000
# spring.datasource.hikari.connection-timeout=20000
# spring.datasource.hikari.max-lifetime=1800000
# spring.datasource.hikari.validation-timeout=5000
# spring.datasource.hikari.leak-detection-threshold=0
# spring.datasource.hikari.initialization-fail-timeout=-1

# spring.jpa.hibernate.ddl-auto=update


# Ek file ka maximum size (e.g., 10MB)
spring.servlet.multipart.max-file-size=10MB

# Ek puri request (jis mein multiple files hon) ka total size
spring.servlet.multipart.max-request-size=10MB



# -----------------------------
# CORS & Auth Redirects
# -----------------------------
app.cors.allowed-origins=http://localhost:5173
app.auth.success-redirect=http://localhost:5173/auth/success
app.auth.failure-redirect=http://localhost:5173/auth/failure

# -----------------------------
# Logging
# -----------------------------
logging.level.org.springframework=INFO
logging.level.com.careerkey=DEBUG




# -----------------------------
# Spring Boot Actuator
# -----------------------------
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=always
management.endpoint.health.show-components=always
management.endpoints.web.base-path=/actuator






# Upload path for degree request documents
degree.doc.upload-path=src/main/resources/static/AttestationDoc







# Sab packages ke liye DEBUG level enable karein
logging.level.root=INFO

# Apne package ke liye detailed logging
logging.level.com.crear=DEBUG
logging.level.org.springframework.web=DEBUG
logging.level.org.springframework.security=DEBUG
logging.level.org.hibernate=INFO
logging.level.org.springframework.transaction=DEBUG

# Request/Response logging
logging.level.org.springframework.web.filter.CommonsRequestLoggingFilter=DEBUG

# Format improve karein
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} - %logger{36} - %msg%n

# File mein bhi log save karein
logging.file.name=logs/application.log
logging.file.max-size=10MB
logging.file.max-history=10




# HEC Auto Registration Configuration
hec.auto-registration.enabled=true
hec.auto-registration.email=${HEC_ADMIN_EMAIL:admin@hec.gov.pk}
hec.auto-registration.password=${HEC_ADMIN_PASSWORD:Admin@123}
hec.auto-registration.name=HEC Pakistan
hec.auto-registration.hec-code=HEC-PK-001
hec.auto-registration.head-office=Islamabad, Pakistan
hec.auto-registration.digital-seal-path=/certificates/hec-seal.pfx

# .env file
HEC_ADMIN_EMAIL=admin@hec.gov.pk
HEC_ADMIN_PASSWORD=SecurePass@123
HEC_AUTO_REGISTRATION_ENABLED=true

# Agar nahi chahte toh disable kar sakte hain
# hec.auto-registration.enabled=false
