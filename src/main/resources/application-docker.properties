# # -----------------------------
# # Docker Profile Settings
# # -----------------------------
# server.port=9090

# # -----------------------------
# # Database Configuration (Docker MySQL)
# # -----------------------------
# # 'db' wahi naam hai jo hum Docker Compose mein MySQL service ko denge
# spring.datasource.url=jdbc:mysql://db:3306/railway?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
# spring.datasource.username=root
# spring.datasource.password=root

# spring.datasource.hikari.pool-name=docker-hikari
# spring.jpa.hibernate.ddl-auto=update

# # -----------------------------
# # IPFS Configuration (Docker context)
# # -----------------------------
# # Agar aapka IPFS bhi docker mein hai to yahan host name ayega, warna ye settings:
# ipfs.mode=local
# ipfs.local.gateway.url=host.docker.internal
# ipfs.gateway.url=host.docker.internal

# # -----------------------------
# # CORS & Auth Redirects (Frontend URL)
# # -----------------------------
# # Frontend agar dost ke machine par chal raha hai to localhost hi rahega
# app.cors.allowed-origins=http://localhost:5173
# app.auth.success-redirect=http://localhost:5173/auth/success
# app.auth.failure-redirect=http://localhost:5173/auth/failure

# # -----------------------------
# # Logging & Management
# # -----------------------------
# logging.level.org.springframework=INFO
# management.endpoints.web.exposure.include=health,info
